SELECT BRANCH_ID, TOTAL_SALES AS '매출액'
FROM (
         SELECT e.BRANCH_ID, SUM(s.PRICE) AS TOTAL_SALES
         FROM EMPLOYEES e
                  LEFT JOIN SELLINGS s ON e.ID = s.EMPLOYEE_ID
         GROUP BY e.BRANCH_ID
     ) AS SALES
ORDER BY TOTAL_SALES DESC;